<%= form_for(@event) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %><br>

	<%= f.label :description %>
	<%= f.text_field :description %><br>

	<%= f.label :happen_at %>
	<%= f.date_field :happen_at %><br>


	<%= f.submit "Submit", remote: true %>
<% end %>
