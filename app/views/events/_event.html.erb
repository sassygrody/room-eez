<%= form_for(@event) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.label :happen_at %>
	<%= f.date_field :happen_at %>


	<%= f.submit "Submit" %>
<% end %>